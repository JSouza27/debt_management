export enum InstallmentStatus {
  PENDING = 'Pending',
  PAID = 'Paid',
}

export enum DebtStatus {
  ACTIVE = 'Active',
  PAYD_OFF = 'Paid off',
  OVERDUE = 'Overdue',
}

export enum OrderBy {
  ASC = 'ASC',
  DESC = 'DESC',
}
